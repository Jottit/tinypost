{
  "private": true,
  "scripts": {
    "build": "esbuild src/jot.js --bundle --format=esm --minify --outfile=static/js/jot.js"
  },
  "dependencies": {
    "@jottit/jot": "^0.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
